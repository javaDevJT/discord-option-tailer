HTTP_BIND=127.0.0.1
HTTP_PORT=8787
# Remote browsers can paste the returned localhost URL into Setup to finish.
RELAY_ROBINHOOD_REDIRECT_URI=http://127.0.0.1:8766/callback
DASHBOARD_USER=relay
# Choose a private password before starting; use it to open the dashboard.
DASHBOARD_PASSWORD=
CODEX_VERSION=0.153.4
PLAYWRIGHT_VERSION=1.62.0
